		<!-- ### rendered into side menu ACCORDION -->
		<div id="sidemenu" class="basic">
			<h3 class="ui-widget-header"><a href="#">Servers</a></h3>
			<div>
				<%= render 'servers/menu' %>
			</div>
			<h3 class="ui-widget-header"><a href="#">Groups</a></h3>
			<div>
			</div>
			<h3 class="ui-widget-header"><a href="#">Tasks</a></h3>
			<div>
			</div>
			<h3 class="ui-widget-header"><a href="#">Histories</a></h3>
			<div>
			</div>
			<% if current_user.admin %>
			<h3 class="ui-widget-header"><a href="#">Admin</a></h3>
			<div>
			</div>
			<% end %>
		</div>

		<script>
			$(document).ready(function() {
				$( ".side-menu" ).button({
					create: function(event, ui) {
						$( ".side-menu" ).removeClass("ui-corner-all");
						$( ".side-menu" ).addClass("hcu-side-button");
					}
				}).click(function() {
					var url = $(this).attr("value"),
						label = $(this).attr("name");
					$("#tabs").tabs("add", url, label);
				});
			});
		</script>

		<style>
			.hcu-side-button { margin:0; width:100%; border:1px 0 solid #888; }
			.hcu-side-button { background:#eee !important; }
			.hcu-side-button span { font-size:0.8em; font-weight:normal; }
			.hcu-side-button span { text-align:left; }
			.hcu-side-button span { line-height:1; color: #333; font-style:italic; }
		</style>
		<!-- vim: set ts=2 sw=2: -->
